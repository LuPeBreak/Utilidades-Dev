const numeros = [100, 200, 300, 400, 500, 600]

// podemos utilizar o for para passar por todos os valores de um array tambem usando o metodo array.length ( que retorna o tamanho do array )
for (let i = 0; i < numeros.length; i++){
            // numeros[0]
  console.log(i, numeros[i])
}

// mas como ja vimos anteriormente podemos utilizar tambem o metodo proprio para arrays o forEach terao mais exemplos futuramente